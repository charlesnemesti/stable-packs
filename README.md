# Stable Packs

Frontend de **Stable Packs** en **Stable Chain** (chain ID `988`).

Pago solo en **USD0**. Un pack: Treasury Desk.

## Local

```bash
npm start
```

Abre [http://localhost:4180](http://localhost:4180).

## Deploy

Estático desde `site/` vía Vercel (`vercel.json`).
